/** Nome do cookie de sessão. Módulo sem dependências — seguro no Edge (middleware). */
export const SESSION_COOKIE = "avanca_session";

/** Duração da sessão. */
export const SESSION_MAX_AGE_SECONDS = 60 * 60 * 24 * 30; // 30 dias
